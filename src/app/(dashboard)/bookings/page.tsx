import{BookingWorkspace}from"@/components/bookings/booking-workspace";import{getBookingWorkspace}from"@/lib/services/bookings";
export default async function BookingsPage(){const data=await getBookingWorkspace();return <div className="space-y-6"><div><p className="text-xs font-semibold uppercase tracking-[.2em] text-primary">Rental lifecycle</p><h1 className="mt-2 text-3xl font-bold">Bookings</h1><p className="mt-2 text-sm text-muted-foreground">Search live availability and reserve vehicles with immutable pricing.</p></div><BookingWorkspace data={data}/></div>}
